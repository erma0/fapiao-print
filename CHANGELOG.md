# 📋 更新日志

> 本分支（`web`）独立发布，与主分支（`master`，Tauri 桌面版）并行维护。
> 下文仅记录本分支的纯前端 Web 版变更历史。

## v3.4.4 — 数电票字段匹配修复（购销方互换 / 发票号码取到信用代码）

### 🐛 修复

- **数电票 PDF 购买方/销售方互换、发票号码取到信用代码前缀**（同步桌面版 `0153c51`）：京东数电专票实测 —— 发票号码显示成 `91110302562134916`（销售方信用代码前缀）、购买方显示成 `售方信息`（竖排页眉碎片）、销售方尾部粘上 `方信统一社会`。根因在文字层的字段匹配层（不是坐标解析）：
  - **`_normText` 未折叠多重空格、未去首尾空白**：PDF 文字层按 Tj 分段时词文本常带空格（`"名 "` / `"售 方 信 息"`），单次全局 CJK 空格折叠会漏掉重叠相邻片段 → 「名称」标签匹配全灭、`_extractNamesByCoords` 直接返回；坐标法失效后字段回落到**内容流顺序文本**（卖出方在前）→ 购销互换。现改为循环折叠 + trim
  - **发票号码跨行兜底只取标签之后的第一段 8~20 位数字**：乱序文本下首个数字串正是信用代码前缀。改为扫描标签后全部数字串取最长，跳过紧邻字母（信用代码尾段 `…34916R`）与紧邻 `.`/数字（小数、被 `{8,20}` 截断的长数字）
  - **`_cleanName` 未拦截纯标签字碎片**：新增「只由标签字构成」的拒绝规则（拦掉 `售方信息` 这类竖排页眉碎片）
  - **`_extractSeller` 名称区混入竖排页眉单字与信用代码标签**：新增 `_isLabelFragmentWord()` 按词过滤，公司名匹配改为**词级优先**，整段文本正则降为最后兜底并收紧量词 `{2,25}`（后缀表含「行/会/社」等单字后缀，贪心匹配会把 `统一社会` 吃进名称尾部）

### ✅ 验证

真实样本 PDF → 文字层 JSON → `extractByCoordinates()` 三场景全绿（发票号码 / 购销双方名称+信用代码 / 金额 176.07·155.82·20.25 / 开票日期 / 专票判定）：

| 场景 | 结果 |
|------|------|
| 内容流顺序文本（数电票真实形态） | 全部字段正确 |
| 视觉顺序文本（正常版式 / 阅读顺序 OCR） | 全部字段正确 |
| 类 OCR 融合行框 + 乱序（模拟 PP-OCR 行框与检测顺序） | 全部字段正确 |

---

## v3.4.3 — 发票类型识别修复 + 筛选专票/普票粒度

### ✨ 新功能

- **类型筛选升级专票/普票粒度**（同步桌面版 v2.6.3）：类型筛选栏的「发票」一档拆分为「专票」「普票」两档，匹配统一走 `resolveInvoiceType` 单一真源，对 XML/OFD 解析、PDF 文字层等非 OCR 来源同样生效；旧配置残留的 `vat` 值按「全部」处理
- **「财政」统一为「非税」，新增「其他」档**（同步桌面版）：非税票据的官方叫法是「非税」，按钮文案统一；新增「其他」档收录**未识别出类型、或识别到但无法归入专票/普票的粗粒度串**（如裸「电子发票」）的发票——此前这些票只能混在「全部」里，配合全选可单独分批排版打印

### 🎨 优化

- **左侧列表工具栏与筛选区 sticky 固定**（同步桌面版）：滚动长列表时，工具栏（全选/计数/操作按钮）与筛选区钉在列表顶部不再随内容滚走，随时可切换筛选与全选
- **文件选择框收窄**：`accept` 去掉 `.tiff/.tif` —— 浏览器本就不支持 TIFF 解码，此前选中必加载失败；webp/gif 浏览器可正常渲染，保留

### 🐛 修复

- **汇总表「发票类型」把所有普通发票显示成「增值税发票」**（同步桌面版 v2.6.3，issue #35）：该列此前不是识别结果 —— XML / OFD 之外一律返回写死的兜底字符串「增值税发票」，普通发票和专用发票在列里完全一样；「增值税发票」在会计语境里又常被读作专票，于是表现为「所有普通发票都被识别成专用发票」。现在改为单一真源 `resolveInvoiceType(f)`：分类标记（通行费 / 车票 / 非税票据）→ 结构化或文字层识别结果 → 兜底「发票」，不再编造类型
  - **专票 / 普票归一化**：新增 `normalizeInvoiceType()` —— 「电子发票(普通发票)」「增值税普通发票」「普通发票」统一显示为 **普票**，含「专用」的显示为 **专票**；汇总表、列表票种标签、右键「复制发票信息」、CSV 导出共用同一口径
  - **统一票种标签，不占格式徽章**：缩略图角标的 `pdf / jpg / ofd / xml` 格式徽章保持原位；所有发票类型改用**同一种票种标签**（文件名下销售方行首的小圆角 chip，按类型配色：专票金 / 普票灰 / 通行费青 / 车票蓝 / 非税紫），通行费、车票、非税不再靠销售方行变色表达；悬停显示原始识别串，无类型信息或无法归类（如裸「电子发票」）不渲染；车票的销售方行只显示标签本身（其名称位存的就是车票标签，避免重复）
  - **PDF 文字层补专普识别**：此前只分 发票 / 车票 / 通行费 / 财政，不区分专普。新增 `_detectVatSubtype()`：票头标题区（`ny < 0.18`）优先、内容流顺序全文兜底、词序拼接三层判定，结果写回 `fileObj.invoiceType`（已有结构化类型时不覆盖）
  - **文字层坐标失效的 PDF 根治**（同步桌面版；实测数电专票样本整页并成 1 行、词按 x 重排）：PDF.js 文字层同理补 `_betterCoordName` 候选合成——坐标邻近提取在拆字 PDF 上会截断/混入标签冒号，跨行提取候选预跑后择优修复，干净坐标值不变
  - **文字层坐标失效的 PDF 也能判出专普**（同步桌面版）：部分数电票 PDF 文字层坐标解析失败（实测样本整页并成 1 行、词按 x 重排），连续标题被拼接打散。增加内容流顺序的原始文本作为中间兜底：票头区 → 原始全文 → 词序拼接
- **双击弹窗（发票选项）信息补全**：顶部信息条改为「文件名 + 统一票种标签 + ✓已打印 / ⚠重复 + 格式·大小」（票种明细悬停标签可见，不再单设一行与头部重复）；新增「备注」可编辑行（与汇总表备注列共用 `f.note`，确认后写回）
- **OFD 判定改「普通」优先**：`ofd-client.js` 新增 `_detectInvoiceTypeTexts()`，模板层 + 数据层文本逐条合并判定（不再「首个命中即 break」）—— 票面其它位置出现「专用」字样、或拆字与文字层噪声拼出的假关键词，都不会把普票判成专票；逐条匹配避免跨文本拼出假关键词，拆字票由拼接串兜底
- **XML 数电票类型增加 LabelCode 兜底**：部分结构变体只有 `<LabelCode>` 没有 `<LabelName>`，现按标准码表判定（01 = 增值税专用发票、02 = 普通发票）；LabelName 存在时仍以名称为准

### 📌 说明

- 通行费发票、车票仍按类别显示（`通行费发票` / 车票标签）：它们也有专普属性，但按类别单独排版，若让专普信息抢先，「通行费发票」会退化成「普票」
- 无法判定类型时显示中性的「发票」，不再误导为「增值税发票」

### ✅ 验证

| 用例 | 结果 |
|------|------|
| `node --check`（app / ofd-client / xml-client / pdf-text） | 全部通过 |
| 函数级用例 23 例（真源优先级 / 统一票种标签五类配色 / 销售方行 / 专普互斥） | 全部通过 |
| 专普判定回归 8 例（含乱序词 + 内容流兜底的真实缺陷场景） | 全部通过 |
| 真实样式截图对照（列表 / 弹窗，Chromium） | 五类票种同形异色、非税与专票不再撞色、格式徽章保持原位、弹窗无重复类型行 |

---

## v3.4.2 — 修复「裁剪白边」在保存/打印中失效

### ✨ 新增

- **裁剪留边可配置**：「排版 → 辅助选项」的「裁剪白边」下方新增「留边」输入框 —— 裁剪时向外保留的像素数，默认 **3**（≈0.25mm），范围 **0–60**（≈0–5mm，上限取 60 是因为再大就失去"裁掉白边"的意义，也避免极端值让裁剪框反转）。改动后自动清空白边缓存并按新值重算，随设置持久化

### 🐛 修复

- **裁剪白边只进了预览，进不了 PDF**：PDF 源发票走矢量嵌入（`embedPage`）时直接嵌入了整页原件，裁剪结果只在预览和位图回退路径生效，于是"预览白了、打印没白"。现在改为按预览检测到的白边范围换算成 PDF 点坐标，用 pdf-lib 的裁剪框做**矢量裁切**——白边被真正裁掉，同时保留矢量文字的清晰度
- **裁剪框换算基准改用 CropBox**：PDF.js 预览渲染的是 CropBox 区域，此前按 MediaBox 换算在「CropBox 与 MediaBox 等尺寸但原点偏移」时会整体错位；同时 CropBox < MediaBox 的页面不再被错杀退回位图，可正确矢量裁切
- **`/Rotate` 页面矢量输出拉伸**：未旋转的内容被按旋转后的尺寸绘制，带 `/Rotate 90/180/270` 的 PDF 矢量输出会错乱。现在把旋转烘焙进 XObject 的 Matrix（与桌面版同款语义，并预补偿 pdf-lib drawPage 的归一化缩放）；无法安全烘焙（MediaBox/CropBox 不一致或原点偏移）时退回位图
- **裁剪后按内容比例适配**：预览、拖拽范围约束、九宫格对齐与 PDF 导出统一走 `getObjDims()`，裁剪后的发票按裁剪尺寸铺满槽位，不再残留一圈白边；「自动旋转」也改为按裁剪后的内容比例判断方向
- **图片发票不再被拉伸**：位图路径改用裁剪后的像素尺寸换算排版尺寸，此前按原图尺寸计算会把裁剪图拉伸回原比例
- **关闭裁剪白边无效**：打印缓存键漏了 `trimWhite` 开关和每票的裁剪状态，关掉开关（或裁剪尚未跑完就打印）时会复用旧的裁剪结果
- **浅灰细字被当白边裁掉**：白边检测阈值 245 会把发票右侧「下载次数：1」这类 250 上下的浅灰细字算作白边（合成图实测复现：灰度 250/252 直接被裁到主体边界）。阈值提到 **253**，并要求一行/列**至少 2 个非白像素**才算「有内容」以抑制孤立浅色噪点（电子发票白底是纯白 255，放宽阈值无副作用）
- **印章/文字边缘被裁掉一点点**：内容边缘经「抗锯齿 + JPEG」后会有一圈 253~254 的淡像素，原阈值 252 把它判为白 → 检测边界内缩；且向外留边距（pad）只有 4px（≈0.34mm）。改为检测准确 + pad 收敛：**四边统一 3px（≈0.25mm）**（曾按方向加大兜底，会让没有「下载次数」字段的发票白白多留白边）
- **左右两侧白边收不干净（pad 兜底 + 列检测范围）**：右侧曾单独设 28px pad 兜「下载次数」，但那是**无差别**的 —— 没有该字段的发票会白白多留 2.4mm。同时列统计此前限定在行检测的 `top..bottom` 内，位于该范围外的左右内容（如超出主体行范围的竖排浅字）会被漏检、边界内缩。现改为**列统计扫全高（0..h）** + 四边统一 3px，实测（4855 真实样本）四边保留均为 0.25mm
- **白边检测分方向阈值**：左/上仍有空白是因为页面边缘的浅灰渐变（R=G=B≈252）被 253 的宽松阈值当成了内容。改为**上下方向用严格阈值 245**（只认彩色/深色内容 —— 红章是彩色的 min 通道很低照样保留，纯灰渐变被正确忽略），**左右方向保持 253**（继续保护「下载次数」这类浅色小字）
- **加载发票后没自动裁剪**：「裁剪白边」开关是跨会话记忆的，但批量加载完成后没有任何触发点（此前只在切换开关/还原/增强时裁剪），表现为"开关是开的、加载后却没裁"。在加载收口点新增 `maybeAutoTrim()` 自动补裁剪
- **裁剪后垂直贴顶**：裁剪白边后图片宽高比变化，`contain` 适配后在槽位内垂直居中，上方留白看起来像"白边没裁掉"。现改为开启「裁剪白边」时**垂直贴顶**（水平仍居中），预览与 PDF 导出同步
- **边缘细线"厚度过滤"已回退**：曾为解决"页面边缘浅灰细线顶住上下白边"引入 `MIN_CONTENT_THICKNESS`（浅灰内容需连续 ≥4 像素），但它会误伤**横排文字的左右边界**（笔画在水平方向不连续），把「下载次数」又裁掉，故回退为「阈值 252 + 每行/列至少 2 个非白像素」。代价：页面边缘若有浅灰细线，上下白边会保留（实测数据见下表）

### ✅ 验证

用带大块白边的合成 A4 PDF 走完整链路（PDF.js 渲染 → 裁剪 → 矢量嵌入 → 重新渲染）实测：

| 检查项 | 结果 |
|--------|------|
| 源页面内容距边 | 左 100 / 上 42 / 右 95 / 下 599 pt |
| 裁剪后矢量输出尺寸 | 402.24 × 203.52 pt（与预览像素换算偏差 0.000） |
| 裁剪后内容距边 | 左 1 / 上 2 / 右 1 / 下 0 pt |
| 未开裁剪 | 仍嵌入整页矢量 595.28 × 841.89 pt |
| `/Rotate` + 裁剪 | 无法安全换算坐标时自动退回裁剪位图 |
| `/Rotate 0/90/180` 矢量 vs 位图端到端 | 构造 `/Rotate` 源 PDF，矢量输出与位图输出（=预览基准）的墨迹四边距差 ≤ 1pt；`/Rotate 90` 页面尺寸正确互换为 841.9 × 595.3 pt |
| 浅灰细字保住（阈值 252 + 最少2像素） | 灰度 244/246/248/250/252 均被保留（旧 245 阈值只能到 248）；254 仍视为白 |
| 噪点抑制 | 主体右侧撒 400 个 246~252 灰度孤立噪点，裁剪框右边界仍停在主体边界 + padding（未被撑大） |
| 裁剪后贴顶（预览，真实应用） | 槽位 477px / 图片 445px → `wrapper.top = 0`（留白全在底部 32px）；未裁剪时行为不变 |
| 裁剪后贴顶（PDF 导出） | 墨迹顶距页顶 14.0pt ≈ 槽位顶 14.2pt（若居中应为 33.2pt） |
| 印章边缘保住 | 含淡边缘（253）的红色印章、章顶 y=760 → 裁剪上边界 742（含 12px pad），章顶未被裁 |
| pad 精确 | 主体上边界 900 → 裁剪上边界 888（= 900−12） |
| 加载后自动裁剪 | 开关为开时批量加载完成 → `trimmedUrl` 自动生成；开关关闭时不触发 |
| pad 四边统一 3px | 主体 + 右侧 250 灰度浅灰字 → box {x:397, y:897, x+w（完整含浅灰字，仅 +3px）, y+h}，四边 pad 一致 |
| 真实样本四边保留（4855 无锡天鹏菜篮子） | 内容 bbox 左 51/上 70/右 2473/下 1569 px → 裁剪后四边保留均 **3px = 0.25mm**（此前右侧 2.6mm、下方 1.0mm）|
| 列统计全高 | 竖排浅字 y 范围超出主体行范围仍被检出（限界检测会漏）；4855 全高与限界一致（= 内容 bbox）无干扰 |
| 裁剪留边配置项 | pad=0/3/20/60 → 裁剪框 x/y 精确等于「检测边界 − pad」；输入 999→60、−5→0、`abc`→3（回退默认）、12→12；`saveSettings` 后 localStorage 中 `feat.trimPad` 正确持久化 |
| 分方向阈值（上下严格） | 页面上下 2px 纯灰横线（R=G=B=252）→ 被忽略，上下白边裁到主体（888/1195）；右侧浅灰字与自动裁剪回归 ✓ |
| 边缘浅灰细线（回退后的已知行为） | 页面上下 2px 浅灰横线会被当作内容 → 上下白边保留（top 停在 295 而非 896）；用户确认此项暂不处理 |

### ⚠️ 已知限制

- `/Rotate` 页面叠加「裁剪白边」、或页面带 CropBox 原点偏移/尺寸不一致时，无法安全换算/烘焙，会退回位图嵌入（仍有正确显示，但失去矢量清晰度）
- 该缺陷为 `web` 分支引入（矢量嵌入 `embedPage` 上线时），`master`/`dev` 桌面版走 iframe 位图打印，不受影响

***

## v3.4.1 — 滚轮防误触 + 右键菜单统一 + 预览区重置入口

> 对齐桌面版 v2.6.1（issue #33）。

### ✨ 新功能

- **右键菜单两处统一**（issue #33）：列表项与预览槽位共用同一份完整菜单——排版份数、旋转 90°、复制发票信息、单票调整（↩ 重置此票 / ⊙ 居中 / ⧉ 应用到全部 / ↩ 重置全部调整）、✕ 删除；右键槽位即选中并同步列表目标文件，不参与排版的文件（如 XML 数电票）自动隐藏单票调整组
- **选中槽位浮动工具条**（issue #33）：选中票面上方浮现「↩ 重置 / ⊙ 居中 / ⧉ 应用到全部」，随槽位定位、随内容滚动，槽位贴视口顶部时自动改放票面内侧；拖拽/缩放期间自动隐藏，松手恢复
- **重置全部单票调整**（issue #33）：一键把所有发票的缩放/偏移恢复默认，与「应用到全部」对应的反向批量入口（右键菜单内）
- **恢复默认拆两档**（issue #33）：设置页新增「↩️ 仅恢复版面」，只重置排版页（纸张/行列/边距/间距/水印等），不影响主题与偏好；原「🔄 恢复默认」行为不变
- **首次选中提示**（issue #33）：第一次选中票面时 toast 提示「双击票面可重置单票调整，右键票面有更多操作」，跨会话仅提示一次

### 🐛 修复

- **侧栏滚轮误改设置**（issue #33）：滚轮增减改为焦点门槛——点击激活输入框/滑块后才响应滚轮，滚动浏览侧栏不再被吞掉滚动并悄悄改值

***

## v3.4.0 — 右键菜单 + 副本标记 + 交互优化

> 对齐桌面版 v2.6.0 的交互改进。

### ✨ 新功能

- **文件列表右键菜单**（issue #32）：右键任意发票直接操作该项，无需「取消全选→勾选→设置→恢复全选」
  - 菜单项：排版份数 ×1/×2/×3、旋转 90°、复制发票信息、删除
  - 复制发票信息：识别到的非空字段逐行拼接（类型/号码/日期/购买方/销售方/金额/税额），一键粘贴到聊天或表格
  - 右键同步选中态：列表高亮 + 预览翻页定位到该发票，不改变勾选状态
  - 输入框内右键保留浏览器原生菜单（复制/粘贴），其余区域屏蔽默认菜单
- **预览副本标记**（issue #32）：辅助选项新增开关，开启后同发票的多个副本在槽位左上角显示 `n/N` 序号（如 1/2、2/2），跨页也能一眼确认副本分布；仅预览显示，不进打印 PDF

### 🎨 优化

- **侧栏工具栏**：全选按钮移至最左，功能区靠左与计数文字换位，逻辑更顺手（#32）
- **份数按钮图标**改为「框+N」样式，与复制图标明确区分（#32）
- 右键/预览槽位交互过滤非左键操作，避免右键误触发拖拽选中

***

## v3.3.0 — 列表拖拽 + 版面留白

> 对齐桌面版 v2.5.1 的列表拖拽排序与 v2.2.2 的槽位精准上传/留白功能。

### ✨ 新功能

- **列表拖拽排序**：拖到列表项边缘 = 顺位插入，拖到中间 = 两张对调；列表筛选状态下自动禁用（显示序 ≠ 底层序）；条目间隙 80px 内吸附最近项，避免插入指示时有时无

- **版面空槽精准上传**：点击哪个格子，新文件就固定出现在哪个格子（#10-①）；目标槽位在已有发票之后时，中间空隙自动补空白占位

- **尾部空槽临时占位留白**：按下版面尾部第一个空槽拖动即临时创建占位进入拖拽链路，松手落在实体之间 = 中间留白；未移动 / 拖回尾部 = 自动取消。占位不参与打印与金额统计，重启不恢复

- **空白占位管理**：占位在列表中显示「空白」徽章 + 上移/下移/删除按钮（双视图均支持）；版面占位槽显示斜纹底 + ＋ 号，点击上传可替换该占位（保留其他留白）；占位不参与勾选、全选、去重、打印标记与金额汇总

***

## v3.2.0 — 识别对齐 + 交互功能补齐

> 对齐桌面版 v2.4.x 的识别引擎与可移植交互功能。

### ✨ 新功能

- **通行费发票识别**：ETC/高速公路通行费电子发票专属识别（toll 标记 + 青绿徽章），销售方保留路桥公司名，汇总表类型列显示「通行费发票」(#25)

- **文件列表方格视图**：缩略图卡片 + hover 操作条（上移/下移/旋转/删除），列表/方格一键切换并持久化

- **重复发票检测**：发票号 / 销售方+金额+日期 双 key 识别，⚠重复徽章 + 「重复」筛选一键勾选重复项 + 安全删除（仅删按发票号判定的可靠重复，疑似重复仅标记人工核对）

- **版面拖拽排序**：拖到发票边缘 = 顺位插入，拖到中间 = 两张对调（双手势，与桌面版 #24 一致）

- **预览滚轮翻页**：普通滚轮触底/触顶自动翻页，150ms 节流防触控板惯性

- **列表与版面双向联动**：点击列表项跳页并选中槽位；选中槽位反向高亮定位列表项

- **列表打印✓标识**：已打印发票显示绿色对勾圆点

### 🛠️ 修复（识别引擎对齐桌面版 v2.4.0）

- **车票检测加严**：弱信号双组确认（13 组关键词命中 ≥2 才判车票），防止增值税/通行费发票因偶发词（如「进站」「乘车」）误判为车票

- **年份/金额判定精细化**：¥ 前缀一律视为金额；纯整数 1900-2099 才算年份；仅 `YYYY.01-12` 才算日期——修复 `¥2000.00` 被误判为年份导致金额丢失

- **车票类型标签细分**：新增「火车票」标签（铁路电子客票 / 火车票 / 出租车票 / 网约车票四级）

- **徽章判定统一**：非税徽章改用 `_isNonTax` 标志判定（原 invoiceType 字符串推断）

- **调试日志清理**：移除原始文本/词表 dump 与信用代码追踪日志

- **移除 isTicketText 死代码**

### 📝 其他

- **版本号一致性**：package.json `1.0.0` → `3.2.0`（与徽章/状态栏版本统一）

- **perPage 统一入口**：分页计算统一走 `getPerPage(s)`，为后续报销单分段等布局扩展铺路

***

## v3.1.0 — 功能补齐 + 可靠性修复

> 补齐 v3.0.0 遗漏的功能说明，修复多项 PDF 打印可靠性问题。

### ✨ 新功能

- **保存图片 PDF 按钮**：非矢量模式，跳过 pdf-lib 字体嵌入，无需下载 CJK 字体即可输出带水印/页脚的 PDF（内容为位图）

### 🛠️ 修复

- **水印/序号 PDF 位置对齐预览**：水印改为 per-slot 居中、序号改为 slot 右上角黑底白字，与预览完全一致

- **CJK 字体嵌入增强**：新增 TTC (TrueType Collection) 字体解包支持，系统本地字体为 TTC 格式时自动提取第一个字体

- **CJK subset 失败兜底**：不再只处理 CFF/OTF 格式异常，任何 subset 失败均无条件重试无 subset 嵌入

- **模块异步加载竞态修复**：新增 `waitForGlobal()` 轮询等待机制，消除 `__pdfClient`/`__ofdClient` 等异步模块未就绪的时序问题

- **批量加载进度优化**：并发加载每张发票，逐个 toast 更新实时进度而非定时轮询

- **vendor 路径修复**：pdf-client.js 中 CMap/worker 路径改为正确的 `../vendor/` 相对路径

- **PDF 缓存键补全**：新增 `number`/`watermarkColor`/`watermarkAngle`/`customFM` 字段到缓存键，消除开关切换时返回过期 PDF 的问题

### 📝 文档

- **README**: 补充打印状态追踪、日期排序、排版份数、调整记忆、设置持久化、主题切换等功能描述

- **README**: 版本号更新至 v3.1.0，桌面版引用更新至 v2.0.8

- **README**: 新增「与桌面版的差异」章节，明确纯 Web 版因浏览器限制不可用的功能

- **README**: 移除不准确的「文件记忆」描述（纯前端无法恢复本地路径）

***

## v3.0.0 — 纯前端（零后端）

> 完成从 Rust 版到纯前端版的最终迁移。所有功能在浏览器内自给自足，无需任何服务器。

### 🗑️ 删除

- **Rust 后端全部代码**: `src-tauri/` 目录、`Cargo.toml`、`Cargo.lock`、所有 Rust 依赖

- **invoice-engine OFD 解析器**: Rust 版 OFD 引擎（已完全移植到 ofd-client.js）

- **OCR 引擎**: MNN / PP-OCRv5 模型文件全部删除

- **Docker / Nginx / systemd**: 部署配置文件全部删除（不再需要服务器）

### 🧹 偏好设置清理

- **删除「保存后自动打开」**(autoOpenPdf): 死开关

- **删除「记忆发票列表」**(fileListMemory): 纯前端无法恢复本地文件路径

- **删除「发票查验」板块**: 仅跳转外部链接无实际功能

### 🔄 前端解析器（完全移植自 Rust）

- **ofd-client.js**: OFD ZIP→XML→SVG→PNG 300DPI，对齐 Rust invoice-engine 全部逻辑

- **xml-client.js**: XML 数电票 DOMParser 解析

- **xml-utils.js**: 共享 XML 工具，处理命名空间前缀

### ✅ 功能完整性

所有核心功能与 Rust v2.0.7 版对齐：

- PDF/OFD/XML/Image 文件加载与渲染

- 发票字段提取（PDF 文字层 > OFD XML > XML 数电票）

- PDF 矢量直通合成（pdf-lib embedPage，保留印章/签章）

- 汇总表（14 字段可勾选、CSV UTF-8 BOM）

- 单票调整（slotScale/slotOffset、九宫格对齐、滚轮缩放）

- 全部辅助功能（裁切线/编号/边框/水印/页码/页脚）

- IndexedDB 文件缓存

- 设置持久化（localStorage）

***

## 关联项目

- **主分支** **`master`**: 独立维护的 [Tauri 桌面版 v2.0.8](https://github.com/erma0/fapiao-print/tree/master)，使用 Rust 后端 + PDFium 引擎，支持静默打印、打印机选择、批量重命名等桌面级功能。需要本地安装 Windows 客户端的用户请使用该版本。

